# 2026-09-22

## 任务列表默认每页 15 条
- `renderer/js/pages.js`：任务列表 state 默认 `pageSize: 5` → `15`（与执行日志页一致；分页器 limits 已含 15，无需改动）。
- 约定更新：任务列表 / 执行日志默认 15 条每页，分组页仍 5 条（已写入 MEMORY.md）。
- 验证：`node scripts/check.js` 54 项通过；截图验证未完成（本机 GPU 进程起不来 + 沙箱权限未批准），改由用户实机确认。
